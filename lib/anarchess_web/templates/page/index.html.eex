<div class="landing">
  <span><%= link "Create a game", to: Routes.game_path(@conn, :new), rel: "nofollow" %></span>
  <div class="js-joinGame <%= if Enum.any?(@open_games), do: '', else: 'hidden' %>">
  <span><%= link "Join a public game", to: Routes.game_path(@conn, :join), rel: "nofollow" %></span>
  <p>There are currently <span class="js-globalCount">0 players</span> connected</p>
</div>
